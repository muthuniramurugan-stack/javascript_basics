// A for loop is used when the number of iterations is known.
//All three parts (start, condition, update) are written in one line.

// Syntax
/*for (initialization; condition; update) {
    // code
} */

for(let i=1;i<=10;i++){
    console.log(i);
}