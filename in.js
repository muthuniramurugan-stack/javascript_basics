// The in operator is used to check if a property exists inside an object
// object
// An object is a collection of key–value pairs
let user = {
    name:'muthu',
    age :23,
};

console.log('age' in user);  // true
console.log('dob' in user); // false